# The patched vphone-cli fork keeps its own git history, with upstream as
# `origin` so the changes stay diffable. Tracking it from here too would nest
# one repo inside another. See host/README.md.
host/vphone-cli/

bridge/node_modules/
bridge/dist/

# Generated from project.yml by xcodegen.
ios/VPhoneRemote/VPhoneRemote.xcodeproj/
ios/VPhoneRemote/Resources/Info.plist
ios/VPhoneRemote/build/

.DS_Store
